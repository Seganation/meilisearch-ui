# Meilisearch UI Environment Variables

# ============================================
# UI Authentication (Optional)
# ============================================
# Basic authentication for the UI interface
# Leave empty to disable authentication
# Note: For production, consider using a reverse proxy (nginx, Caddy)
# with authentication, or implement OAuth2/OIDC
# Rate limiting should be configured at the reverse proxy level

# Username for UI login
VITE_AUTH_USERNAME=admin

# Password for UI login (minimum 3 characters recommended)
# IMPORTANT: Change this to a strong password in production!
VITE_AUTH_PASSWORD=your-secure-password

# ============================================
# Singleton Mode Configuration (Optional)
# ============================================
# When enabled, the UI connects to a single Meilisearch instance
# Users cannot add/edit/remove instances

# Enable singleton mode (set to "true" to enable)
# VITE_SINGLETON_MODE=true

# Meilisearch server URL (required if singleton mode is enabled)
# VITE_SINGLETON_HOST=http://localhost:7700

# Meilisearch API key (optional, depends on your Meilisearch configuration)
# VITE_SINGLETON_API_KEY=your-master-key

# ============================================
# Base Path Configuration (Optional)
# ============================================
# Set a custom base path for the application
# Useful when deploying to a subdirectory
# Example: /meilisearch-ui
# BASE_PATH=/

# ============================================
# Docker Environment Variables
# ============================================
# When using Docker, use these variables without the VITE_ prefix
# The entrypoint script will transform them automatically

# Docker authentication (without VITE_ prefix)
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your-secure-password

# Docker singleton mode (without VITE_ prefix)
# SINGLETON_MODE=true
# SINGLETON_HOST=http://meilisearch:7700
# SINGLETON_API_KEY=your-master-key

# ============================================
# Development Notes
# ============================================
# 1. Copy this file to .env.local for local development
# 2. Never commit .env.local to version control
# 3. Environment variables are embedded at build time
# 4. In Docker, variables are processed at container startup
# 5. For CORS issues, start Meilisearch with: --http-cors '*'
